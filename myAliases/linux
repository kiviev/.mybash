#### directorios
projects="$HOME/projects"


#### alias

alias update='sudo apt-get update'
alias upgrade='sudo apt-get upgrade'
alias actualizar='update && upgrade && sudo apt-get dist-upgrade -y'
alias instalar='sudo apt-get install'

alias proj="cd $projects"
alias cursos="cd $cursos"


### alias ejecucion programas
alias cc="claude"
alias ccr="cc --resume"	


##### edicion archivos

alias hosts="sudo nano /etc/hosts"


##### conexions ssh



##### comandos terminal

alias lsl="ls -lah"


##### Tailwind

alias ts='sudo tailscale'
alias tsup='ts up'
alias tsdown='sudo tailscale down'
alias tslist='ts exit-node list'
alias tsvps='tsup --exit-node=vps --accept-routes && tslist'
alias tshome='tsup --exit-node=packhome --accept-routes && tslist'
alias tsstatus='sudo tailscale status'

##### Calibre

alias calibre='killall gvfsd-mtp; calibre'


